<div class="header">
  <h3>Simple Airbnb</h3>
  <h3> <%= link_to "Add Flat", new_flat_path %>
</div>


<div class='container'>



  <% @flats.each do |flat| %>
    <div class='card'>
      <p><strong> <%= flat.name %> </strong> </p>
      <p><%= flat.description %> </p>


      <div class='tile'>
        <div class='sub-card'>
          <span> 🧙 <%=  flat.number_of_guests%> </span>
          <span> <strong> £ </strong> <%=  flat.price_per_night%> </span>
        </div>

        <div class="sub-card2">
          <strong><%= link_to "More info", flat_path(flat) %></strong>
          <%= link_to "🗑", flat_path(flat), method: :delete, data: { confirm: "Are you sure?" } %>
        </div>
      </div>

    </div>

  <% end %>


</div>
